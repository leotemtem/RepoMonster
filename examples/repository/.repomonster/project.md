# Project description

This service exposes payment operations through a FastAPI HTTP API. Route handlers coordinate request validation and service calls; domain behaviour belongs in the service layer, and persistence belongs behind repository interfaces.
